<UserControl x:Class="PDM.Win.Views.ProductModel.Create"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:toolkit="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d">
    <Grid  Margin="0, 10, 0, 0" x:Name="CreateModelPanel">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2.3*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <toolkit:WatermarkTextBox Watermark="Product Model Name here" x:Name="txtProductModel">
            <toolkit:WatermarkTextBox.WatermarkTemplate>
                <DataTemplate>
                    <Border BorderBrush="Transparent" BorderThickness="0" Padding="10">
                        <ContentControl Content="{Binding}" Foreground="Black" Focusable="False" VerticalAlignment="Center" />
                    </Border>
                </DataTemplate>
            </toolkit:WatermarkTextBox.WatermarkTemplate>
        </toolkit:WatermarkTextBox>
        <Button Content="Create" Grid.Column="3" HorizontalAlignment="Left" Click="Create_Click">
        </Button>
    </Grid>
</UserControl>
